# Data Pipeline Skill

This skill processes raw data files and prepares them for model training.

## Capabilities
- Load CSV/Parquet files
- Clean and normalize data
- Feature engineering

## Usage
Invoke via the agent orchestrator with `skill: data_pipeline`.
